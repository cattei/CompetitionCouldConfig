spring:
  application:
    name: competition-ribbon
  cloud:
     config:
          lable: master
          profile: dev
  port: 9090
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
debug: true